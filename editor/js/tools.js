// Инициализация инструментов редактора
function initTools() {
    // Инициализируем навигацию по холсту
    initCanvasNavigation();
    
    // Инициализируем мультиредактирование
    initMultiEditFeature();
    
    // Инициализируем расширенные возможности управления объектами
    initAdvancedObjectManagement();
}
